{% extends "::base.html.twig" %}

{% block title %}AddressBookBundle:User:new{% endblock %}

{% block body %}
<h1>Welcome to the User:new page</h1>

{% if formModify is defined %}

    {{ form_start(formModify, {'attr': { 'name':'UserModify' }}) }}
    {{ form_widget(formModify) }}
    {{ form_end(formModify) }}

{% endif %}
{% if formAddress is defined %}
    {{ form_start(formAddress, {'attr': { 'name':'AddressModify' }}) }}
    {{ form_widget(formAddress) }}
    {{ form_end(formAddress) }}
{% endif %}



{% if success is defined %}
    Dodano
    {{ name }}
    {{ surname }}
    {{ desc }}
{% endif %}

{% endblock %}
